# Java Web Template（系统管理上下文）

若依式全栈管理模板的系统管理域：组织、人员、访问控制与会话审计的语言约定。

## Language

### 组织与人员

**部门（Dept）**:
组织树的节点，至多一个父部门；每个用户至多归属一个部门（允许暂无归属）。禁用部门不追溯影响已归属用户的登录与使用。
_Avoid_: 组织、机构、单位

**岗位（Post）**:
与部门正交的职务维度；一个用户可同时担任多个岗位（允许无岗位）。
_Avoid_: 职位、职务

**负责人（Leader）**:
部门的联系人描述，纯自由文本（如姓名、电话），不构成对用户的引用。
_Avoid_: 部门领导、主管用户

### 访问控制

**角色（Role）**:
权限的集合载体：既挂功能权限（权限点），也挂数据范围。
_Avoid_: 用户组、组

**权限点（Perm）**:
功能权限的最小单元，形如 `模块:实体:动作`；与数据范围（管"能看见哪些数据"）相对，管"能不能做这个动作"。
_Avoid_: 功能、操作权限

**数据范围（Data Scope）**:
角色决定用户可见数据集合大小的边界，五档：全部 / 自定义（精确等于勾选的部门集合，不含未勾选的子孙）/ 本部门 / 本部门及以下（含全部子孙部门）/ 仅本人；一个用户持多角色时取最大范围。
_Avoid_: 数据权限（口语可用，正式表述统一用"数据范围"以免与权限点混淆）

**验证码（Captcha）**:
登录时的一次性人机校验图形，短时效、用后即焚；可经参数配置全局开关，默认关闭。
_Avoid_: 滑块、人机验证

**账号锁定（Lockout）**:
同一用户名连续多次密码错误后的临时登录拒绝，到期自动解除，成功登录即清零计数；与管理员对账号设置的"禁用"（永久态）相区分。
_Avoid_: 封禁、冻结

### 会话与审计

**会话（Session）**:
一次成功登录产生的、带唯一标识（jti）的可撤销凭证；过期或被撤销即失效。同一用户可并存多个会话、互不互踢；登出/强退作废对应会话，改密作废该用户全部会话。
_Avoid_: 登录态、token（token 指凭证载体本身）

**在线用户（Online User）**:
当前持有未过期且未撤销会话的用户视图。

**强退（Force Logout）**:
管理员使指定会话立即失效的操作。
_Avoid_: 踢下线

**操作日志（Operation Log）**:
对写操作的业务留痕：操作人、动作、入参、结果、耗时、异常。

**登录日志（Login Log）**:
登录尝试（成功/失败）与登出的留痕。

### 基础数据

**字典（Dictionary）**:
按"类型"归组的枚举登记处：一个类型下有多条数据项（标签 + 键值），供下拉框等消费。
_Avoid_: 枚举、下拉选项

**参数配置（Config）**:
全局键值配置，键唯一；供业务按 key 读取。
_Avoid_: 系统参数

**公告（Notice）**:
管理端维护的通知文本；用户侧经首页公告卡展示（仅启用状态可见）。
